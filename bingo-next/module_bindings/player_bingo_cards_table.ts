// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { PlayerBingoCard } from "./player_bingo_card_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `PlayerBingoCards`.
 *
 * Obtain a handle from the [`playerBingoCards`] property on [`RemoteTables`],
 * like `ctx.db.playerBingoCards`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.playerBingoCards.on_insert(...)`.
 */
export class PlayerBingoCardsTableHandle {
  tableCache: TableCache<PlayerBingoCard>;

  constructor(tableCache: TableCache<PlayerBingoCard>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<PlayerBingoCard> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `cardId` unique index on the table `PlayerBingoCards`,
   * which allows point queries on the field of the same name
   * via the [`PlayerBingoCardsCardIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.playerBingoCards.cardId().find(...)`.
   *
   * Get a handle on the `cardId` unique index on the table `PlayerBingoCards`.
   */
  cardId = {
    // Find the subscribed row whose `cardId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: string): PlayerBingoCard | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.cardId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: PlayerBingoCard) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: PlayerBingoCard) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: PlayerBingoCard) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: PlayerBingoCard) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: PlayerBingoCard, newRow: PlayerBingoCard) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: PlayerBingoCard, newRow: PlayerBingoCard) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
